# Limousines – 500% Optimized Stack

## Deploy (antigravity)
1. Unzip
2. `npm install` (root + each folder)
3. `npm run build`
4. `npm start` (serves backend on :5000 + statics)

## Stack
- Frontend: Vite + React + AVIF + SW
- Backend: Node + Express + Redis
- CI: GitHub Actions
- PWA: Offline-first
