// Placeholder for GPS service logic (e.g., driver location handling)
// In a real implementation this could expose functions to broadcast driver coordinates
export function broadcastLocation(driverId, lat, lng) {
  // TODO: integrate with WebSocket server or external tracking provider
  console.log(`Driver ${driverId} location: ${lat}, ${lng}`)
}
